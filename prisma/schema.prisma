generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native", "rhel-openssl-3.0.x"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id            String    @id @default(cuid())
  clerkId       String    @unique
  name          String?
  email         String?
  age           Int?
  isAdult       Boolean   @default(false)
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  chats         Chat[]
  dailyUsage    DailyUsage?
}

model Chat {
  id          String    @id @default(cuid())
  title       String?
  userId      String
  user        User      @relation(fields: [userId], references: [id], onDelete: Cascade)
  messages    Message[]
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
}

model Message {
  id            String   @id @default(cuid())
  role          String   // "user" | "assistant"
  content       String
  imageUrl      String?
  videoUrl      String?
  fileUrl       String?  // رابط الملف المرفق
  fileName      String?  // اسم الملف المرفق
  modelUsed     String?
  personaUsed   String?  // الشخصية المستخدمة (المفكر، الخبير، إلخ)
  isUncensored  Boolean  @default(false)
  chatId        String
  chat          Chat     @relation(fields: [chatId], references: [id], onDelete: Cascade)
  createdAt     DateTime @default(now())
  fileUrl   String?
}

model DailyUsage {
  id           String   @id @default(cuid())
  userId       String   @unique
  user         User     @relation(fields: [userId], references: [id])
  imagesUsed   Int      @default(0)
  videosUsed   Int      @default(0)
  resetDate    DateTime @default(now())
}
